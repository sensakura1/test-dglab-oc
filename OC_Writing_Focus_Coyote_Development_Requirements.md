# 《OC 设定写作督促工具》开发需求文档

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 文档名称 | 《OC 设定写作督促工具》开发需求文档 |
| 版本 | v0.1 |
| 编写日期 | 2026-07-09 |
| 目标平台 | Windows |
| 目标用户 | OC 设定创作者、写作爱好者 |
| 需求类型 | 写作专注监测、分心提醒、郊狼设备联动 |

---

## 2. 需求概述

本项目拟开发一个本地运行的写作督促工具。用户在编写 OC 设定时，可以设定目标写作窗口、目标文档、允许页面、分心页面、检测时间、触发强度、触发持续时间等参数。

当程序检测到用户长时间离开写作页面、停留在其它页面、无输入、未达到写作进度，或进入用户定义的分心场景时，程序按照配置调用郊狼设备接口进行督促触发。

程序必须提供强度上限、持续时间上限、冷却时间、急停、锁定、模拟测试等安全机制。所有监测逻辑应在本地完成，不上传用户写作内容。

---

## 3. 需求对话

**需求方：**  
之前游戏死亡触发的需求推翻。现在要做一个写 OC 设定时用的督促工具。

**开发方：**  
明白。新的核心目标是“写作专注督促”，不再检测游戏事件。程序会关注用户是否处于设定的写作环境中，以及是否长时间切到其它页面。

**需求方：**  
如果长时间在其它页面，就按设定触发。

**开发方：**  
可以。我们会提供页面/应用范围配置、离开写作页面的判定时间、触发强度、触发持续时间、冷却时间和触发次数限制。

**需求方：**  
需要能设定时间、范围和强度。

**开发方：**  
会支持这些配置：

- 允许写作范围：例如指定软件、浏览器页面、窗口标题关键词。
- 分心范围：例如指定应用、网页标题、网址关键词。
- 离开写作页面多久后触发。
- 无输入多久后触发。
- 强度范围和实际触发强度。
- 持续时间范围。
- 每次触发后的冷却时间。
- 每个写作周期最大触发次数。

---

## 4. 项目目标

### 4.1 核心目标

- 帮助用户在写 OC 设定时保持专注。
- 监测当前活跃窗口是否属于写作范围。
- 监测用户是否长时间停留在其它页面。
- 监测用户是否长时间没有写作输入。
- 在满足触发条件时调用郊狼设备接口。
- 支持配置时间、范围、强度、持续时间、冷却等参数。
- 支持测试触发、急停、锁定和日志记录。

### 4.2 安全目标

- 所有强度和持续时间必须有上限。
- 触发前必须经过规则层校验。
- 急停优先级最高。
- 设备断线、配置异常、接口失败时不得触发。
- 默认使用模拟设备模式，真实设备模式需要用户手动开启。
- 不上传用户文档内容、窗口标题和输入数据。
- 不记录完整写作内容。

---

## 5. 用户角色

| 角色 | 描述 | 主要操作 |
| --- | --- | --- |
| 普通用户 | 使用工具督促自己写 OC 设定 | 开始专注、设置时间、设置强度、急停 |
| 高级用户 | 自定义窗口范围和触发规则 | 配置白名单、黑名单、窗口标题、URL 规则 |
| 测试人员 | 验证规则和设备调用 | 模拟分心、模拟无输入、测试设备触发 |

---

## 6. 核心概念

### 6.1 写作范围

写作范围是用户认为“正在写 OC 设定”的应用或页面。

示例：

- Obsidian 中标题包含 `OC`、`设定`、`角色` 的窗口。
- Word、WPS、Notion、Typora、VS Code 等指定进程。
- 浏览器中 URL 或标题包含指定关键词的页面。
- 用户手动标记为“允许写作”的当前窗口。

### 6.2 黑白名单范围

范围规则改为用户自行维护黑名单和白名单。

白名单是用户认为“允许写作或不应处罚”的应用、窗口、页面标题或 URL。命中白名单时不处罚，并清除离开计时。

黑名单是用户认为“明确分心或禁止停留”的应用、窗口、页面标题或 URL。命中黑名单时直接生成触发事件，不等待常规离开时间。

既不在白名单，也不在黑名单的窗口或页面，按常规离开写作范围规则计时，超过设定时间后触发。

示例：

- 白名单：OC 文档、角色设定、世界观笔记。
- 黑名单：视频网站、社交媒体、游戏启动器。
- 未命中：其它普通页面，按离开时间规则处理。

### 6.3 触发事件

内部统一使用以下事件：

| 事件名 | 说明 |
| --- | --- |
| `FOCUS_LEFT` | 用户离开写作范围超过设定时间 |
| `DISTRACTION_DETECTED` | 用户命中黑名单范围，立即触发 |
| `IDLE_TOO_LONG` | 用户长时间无键盘输入或无写作进度 |
| `SESSION_TIMEOUT` | 当前写作周期超时且未完成目标 |
| `MANUAL_TEST` | 用户手动测试触发 |

---

## 7. 功能需求

### 7.1 程序启动

**需求描述：**  
用户启动程序后，程序加载本地配置，初始化窗口监测、输入监测、设备适配器和安全控制器。

**功能要求：**

- 首次启动生成默认配置。
- 配置损坏时使用安全默认配置。
- 默认不开启真实设备触发。
- 用户需要点击“开始专注”后才进入监测。
- 用户可随时暂停或结束专注。

**验收标准：**

- 首次启动可正常进入主界面。
- 配置异常时程序不崩溃。
- 未开始专注时不会触发设备。

---

### 7.2 写作范围配置

**需求描述：**  
用户可以配置哪些应用或页面属于写作范围。

**功能要求：**

- 支持按进程名匹配，例如 `WINWORD.EXE`、`Obsidian.exe`、`Code.exe`。
- 支持按窗口标题关键词匹配，例如 `OC`、`人物设定`、`世界观`。
- 支持按浏览器 URL 关键词匹配。
- 支持一键添加当前窗口到写作范围。
- 支持多个写作范围规则。
- 写作范围规则支持启用/禁用。

**验收标准：**

- 当前窗口匹配写作范围时，状态显示为“写作中”。
- 当前窗口不匹配写作范围时，开始累计离开时间。
- 用户新增规则后无需重启即可生效。

---

### 7.3 黑名单配置

**需求描述：**  
用户可以配置哪些应用或页面属于黑名单范围。

**功能要求：**

- 支持按进程名匹配。
- 支持按窗口标题关键词匹配。
- 支持按 URL 关键词匹配。
- 支持一键添加当前窗口到分心范围。
- 黑名单命中后立即生成 `DISTRACTION_DETECTED` 事件。
- 黑名单优先级高于白名单。
- 黑名单不等待普通离开时间。

**验收标准：**

- 当前窗口匹配黑名单时，状态显示为“黑名单命中”。
- 命中黑名单后立即生成 `DISTRACTION_DETECTED` 事件。
- 同一黑名单窗口持续存在时不得无限重复触发。

---

### 7.4 活跃窗口监测

**需求描述：**  
程序需要周期性读取当前活跃窗口信息，用于判断用户是否仍在写作。

**功能要求：**

- 读取当前前台窗口进程名。
- 读取当前前台窗口标题。
- 对浏览器可选读取当前 URL。
- 监测间隔默认 `1000ms`。
- 监测逻辑不得明显影响系统性能。
- 用户可关闭 URL 读取，只使用窗口标题匹配。

**隐私要求：**

- 默认不保存完整窗口标题历史。
- 默认不保存完整 URL。
- 日志中只保存命中的规则名称。
- 用户可开启调试模式查看匹配详情。

**验收标准：**

- 能正确识别当前窗口是否属于写作范围。
- 能正确识别当前窗口是否属于分心范围。
- 关闭 URL 读取后，程序仍能按进程名和标题工作。

---

### 7.5 输入与进度监测

**需求描述：**  
程序需要判断用户是否长时间没有进行写作输入。

**功能要求：**

- 支持检测键盘输入活跃度。
- 支持检测鼠标是否长时间无操作。
- 支持仅统计目标写作窗口中的输入。
- 支持设置无输入触发时间。
- 可选支持目标字数或增量字数检测。
- 默认不读取用户输入内容，只记录输入次数和时间。

**验收标准：**

- 用户长时间无输入时生成 `IDLE_TOO_LONG` 事件。
- 用户恢复输入后，无输入计时清零。
- 不记录用户具体输入的文字内容。

---

### 7.6 时间配置

**需求描述：**  
用户可以设置不同场景下的触发时间。

**配置项：**

| 配置项 | 说明 | 默认值 |
| --- | --- | --- |
| `leave_grace_seconds` | 离开写作范围多久后触发 | `300` |
| `distraction_grace_seconds` | 已废弃，黑名单命中立即触发 | `0` |
| `idle_grace_seconds` | 无输入多久后触发 | `600` |
| `session_minutes` | 单次专注周期时长 | `45` |
| `cooldown_seconds` | 两次触发之间的冷却时间 | `180` |

**验收标准：**

- 每个时间项可单独配置。
- 达到对应时间后生成对应触发事件。
- 冷却期间不会重复触发。

---

### 7.7 范围配置

**需求描述：**  
用户可以设置监测范围、允许范围和触发范围。

**范围类型：**

| 范围 | 说明 |
| --- | --- |
| 写作白名单 | 处于该范围时视为正在写作 |
| 分心黑名单 | 命中该范围后立即触发 |
| 忽略名单 | 处于该范围时不计入写作，也不触发 |
| 暂停范围 | 处于该范围时自动暂停监测，例如锁屏或会议软件 |

**验收标准：**

- 白名单、黑名单、忽略名单可分别配置。
- 黑名单命中时立即触发。
- 白名单命中时不处罚。
- 未命中黑名单和白名单时，按常规离开时间规则处理。
- 忽略名单命中时不触发设备。

---

### 7.8 强度与持续时间配置

**需求描述：**  
用户可以设置不同事件对应的设备触发强度和持续时间。

**功能要求：**

- 支持设置默认强度。
- 支持按事件类型设置强度。
- 支持设置强度随机范围，例如 `20 - 35`。
- 支持设置持续时间随机范围，例如 `1000ms - 3000ms`。
- 支持设置最大强度上限。
- 支持设置最大持续时间上限。
- 超出上限时按上限执行。

**默认配置：**

| 事件 | 强度 | 持续时间 |
| --- | --- | --- |
| `FOCUS_LEFT` | `20 - 30` | `1000ms - 2000ms` |
| `DISTRACTION_DETECTED` | `30 - 45` | `1500ms - 3000ms` |
| `IDLE_TOO_LONG` | `15 - 25` | `1000ms - 2000ms` |
| `SESSION_TIMEOUT` | `25 - 40` | `2000ms - 4000ms` |
| `MANUAL_TEST` | `10` | `1000ms` |

**验收标准：**

- 用户可设置每类事件的强度范围。
- 实际触发强度在配置范围内。
- 实际触发强度不超过最大强度。
- 实际持续时间不超过最大持续时间。

---

### 7.9 触发规则引擎

**需求描述：**  
所有事件必须经过规则引擎判断后，才能调用设备。

**规则流程：**

```text
监测模块产生事件
  -> 检查专注是否开启
  -> 检查是否急停锁定
  -> 检查是否处于冷却期
  -> 检查是否超过本周期最大触发次数
  -> 计算强度和持续时间
  -> 调用郊狼设备适配器
  -> 记录触发日志
```

**功能要求：**

- 支持全局冷却。
- 支持每类事件独立冷却。
- 支持每个专注周期最大触发次数。
- 支持连续分心升级强度。
- 支持触发后显示提醒文本。

**验收标准：**

- 冷却期间不会重复触发。
- 达到最大触发次数后不再触发。
- 所有触发均有日志记录。

---

### 7.10 郊狼设备连接

**需求描述：**  
程序需要封装郊狼设备接口，并支持真实设备和模拟设备两种模式。

**接口抽象：**

```ts
interface CoyoteAdapter {
  connect(): Promise<void>;
  disconnect(): Promise<void>;
  activate(options: CoyoteActivation): Promise<void>;
  stop(): Promise<void>;
  getStatus(): Promise<CoyoteStatus>;
}

interface CoyoteActivation {
  intensity: number;
  durationMs: number;
  channel: "A" | "B" | "both";
  pattern: "constant" | "pulse";
}
```

**功能要求：**

- 默认使用 `MockCoyoteAdapter`。
- 真实设备模式必须由用户手动开启。
- 支持配置接口地址、通信方式和配对信息。
- 支持连接状态显示。
- 支持断线重连。
- 支持立即停止。

**验收标准：**

- 模拟设备模式可完整测试触发流程。
- 真实设备模式可连接配置的郊狼接口。
- 设备断线时不会继续发送触发指令。

---

### 7.11 急停与锁定

**需求描述：**  
程序必须提供急停功能，用户可立即停止设备输出。

**功能要求：**

- 提供界面急停按钮。
- 提供全局急停快捷键。
- 默认快捷键为 `Ctrl+Alt+S`。
- 急停后立即调用设备停止接口。
- 急停后进入锁定状态。
- 用户手动解除锁定前，不再触发设备。

**验收标准：**

- 设备触发过程中可立即急停。
- 急停后不会继续响应分心事件。
- 用户可明确解除锁定。

---

### 7.12 测试触发

**需求描述：**  
用户可以在不开始专注的情况下测试配置是否有效。

**功能要求：**

- 提供测试触发按钮。
- 测试触发可选择事件类型。
- 测试触发走完整规则流程。
- 测试触发默认使用低强度。
- 支持只测试规则、不调用设备。

**验收标准：**

- 点击测试按钮可生成 `MANUAL_TEST` 事件。
- 测试触发可查看最终强度和持续时间。
- 模拟设备模式下可查看模拟调用日志。

---

### 7.13 配置管理

**需求描述：**  
程序需要保存用户配置，并在下次启动时自动加载。

**配置示例：**

```json
{
  "enabled": false,
  "device_mode": "mock",
  "session_minutes": 45,
  "leave_grace_seconds": 300,
  "distraction_grace_seconds": 120,
  "idle_grace_seconds": 600,
  "cooldown_seconds": 180,
  "max_triggers_per_session": 5,
  "max_intensity": 60,
  "max_duration_ms": 5000,
  "emergency_stop_key": "Ctrl+Alt+S",
  "privacy": {
    "save_window_titles": false,
    "save_urls": false,
    "read_browser_url": false
  },
  "writing_scope": [
    {
      "name": "OC 文档",
      "process": "Obsidian.exe",
      "title_keywords": ["OC", "设定", "角色"]
    }
  ],
  "distraction_scope": [
    {
      "name": "视频网站",
      "url_keywords": ["bilibili.com", "youtube.com"],
      "title_keywords": ["视频", "直播"]
    }
  ],
  "ignore_scope": [
    {
      "name": "音乐播放器",
      "process": "Spotify.exe"
    }
  ],
  "triggers": {
    "FOCUS_LEFT": {
      "intensity_range": [20, 30],
      "duration_range_ms": [1000, 2000]
    },
    "DISTRACTION_DETECTED": {
      "intensity_range": [30, 45],
      "duration_range_ms": [1500, 3000]
    },
    "IDLE_TOO_LONG": {
      "intensity_range": [15, 25],
      "duration_range_ms": [1000, 2000]
    }
  },
  "coyote": {
    "transport": "websocket",
    "endpoint": "ws://127.0.0.1:8080",
    "pairing_token": ""
  }
}
```

**验收标准：**

- 用户修改配置后可保存。
- 重启程序后配置仍然存在。
- 缺失字段可用默认值补全。
- 配置非法时进入安全模式。

---

### 7.14 日志记录

**需求描述：**  
程序需要记录关键运行日志，方便用户复盘分心情况和排查设备问题。

**日志内容：**

- 专注开始和结束时间。
- 命中的规则名称。
- 触发事件类型。
- 触发强度和持续时间。
- 设备连接状态。
- 急停事件。
- 错误信息。

**隐私要求：**

- 默认不记录完整窗口标题。
- 默认不记录完整 URL。
- 不记录用户输入内容。
- 不记录明文配对 token。

**验收标准：**

- 用户可查看最近触发记录。
- 设备调用失败时有错误日志。
- 日志中敏感信息已脱敏。

---

## 8. 界面需求

主界面需要包含：

- 开始专注/暂停/结束按钮。
- 当前状态：写作中、离开中、分心中、空闲中、冷却中、锁定中。
- 当前活跃规则名称。
- 本次专注剩余时间。
- 离开写作范围计时。
- 分心页面停留计时。
- 无输入计时。
- 强度范围设置。
- 持续时间范围设置。
- 冷却时间设置。
- 每周期最大触发次数设置。
- 写作范围管理。
- 分心范围管理。
- 忽略范围管理。
- 设备模式：模拟设备/真实设备。
- 连接/断开设备按钮。
- 测试触发按钮。
- 急停按钮。
- 最近触发日志。

界面要求：

- 急停按钮必须醒目。
- 真实设备模式需要二次确认。
- 配置非法时应提示具体错误。
- 隐私相关开关应明确显示。

---

## 9. 非功能需求

### 9.1 安全性

- 强度和持续时间必须限制在安全上限内。
- 急停优先级最高。
- 设备异常时不得触发。
- 配置异常时默认进入安全模式。
- 真实设备触发必须由用户主动开启。

### 9.2 隐私

- 不上传用户数据。
- 不保存写作内容。
- 默认不保存完整窗口标题和 URL。
- 日志应只记录规则命中结果。

### 9.3 稳定性

- 程序应能长时间运行。
- 窗口监测失败不应导致程序崩溃。
- 设备接口超时不应阻塞主界面。
- 异常应记录日志并提示用户。

### 9.4 性能

- 活跃窗口监测默认间隔为 `1000ms`。
- 不进行高频图像识别。
- 不监听或保存具体键入内容。
- 程序不应明显影响写作软件、浏览器或系统响应。

---

## 10. 业务流程

```text
用户启动程序
  -> 加载配置
  -> 用户点击开始专注
  -> 程序监测当前活跃窗口
  -> 判断是否在写作范围、分心范围或忽略范围
  -> 统计离开时间、分心时间、无输入时间
  -> 达到触发条件后生成事件
  -> 规则引擎检查冷却、锁定、次数上限
  -> 计算强度和持续时间
  -> 调用郊狼设备适配器
  -> 记录日志
```

---

## 11. 异常流程

| 异常场景 | 处理方式 |
| --- | --- |
| 设备未连接 | 不触发，提示用户连接或切换模拟模式 |
| 配置文件损坏 | 使用安全默认配置 |
| 当前窗口无法读取 | 视为未知状态，不直接触发 |
| 浏览器 URL 无法读取 | 使用窗口标题和进程名判断 |
| 冷却期内再次分心 | 记录事件但不触发设备 |
| 达到周期最大触发次数 | 停止触发，提示用户 |
| 用户触发急停 | 立即停止设备并进入锁定 |

---

## 12. 验收标准

项目完成后，应满足以下条件：

- 程序可以启动并加载配置。
- 用户可以设置写作范围、分心范围和忽略范围。
- 用户可以设置离开时间、分心时间和无输入时间。
- 用户可以设置触发强度范围和持续时间范围。
- 用户可以开始、暂停和结束专注周期。
- 离开写作范围超过设定时间后可以触发。
- 停留在分心范围超过设定时间后可以触发。
- 长时间无输入后可以触发。
- 冷却期间不会重复触发。
- 达到最大触发次数后不会继续触发。
- 急停按钮和快捷键可以立即停止设备。
- 默认不保存写作内容、完整窗口标题或完整 URL。
- 模拟设备模式可完整测试流程。
- 真实设备模式可调用郊狼接口。

---

## 13. 版本规划

| 版本 | 目标 |
| --- | --- |
| v0.1 | 配置管理、模拟设备、手动测试触发 |
| v0.2 | 活跃窗口监测、写作范围和分心范围判断 |
| v0.3 | 输入空闲监测、规则引擎、冷却和次数限制 |
| v0.4 | 接入郊狼接口、急停、日志 |
| v0.5 | 图形界面、范围管理、专注周期统计 |
| v1.0 | 稳定版本，完成测试和打包发布 |
